export enum Route {
  HOME = "/",
  SIGNUP = "/signup",
  SIGNIN = "/signin",
  QUIZ = "/quiz",
}

export enum ApiRoute {
  CREATE_USER = "/users",
  QUESTION = "/questions",
  SUBMIT_ANSWER = "/answers/submit",
}
